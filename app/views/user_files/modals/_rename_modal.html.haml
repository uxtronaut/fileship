= form_modal_tag :id => "rename-file-modal-#{user_file.id}", :success_message => 'File Renamed' do
  .modal-header
    = link_to '&#215;'.html_safe, '#', :data => {:dismiss => 'modal'}, :class => 'close'
    %h2 Rename File
    %h3= user_file.name
  .modal-body
    = render 'user_files/rename_form', :user_file => user_file
  .modal-footer
    = image_tag 'loading.gif', :alt => 'Working...', :class => ['progress-wheel', 'pull-left']
    = link_to 'Rename File', '#', :class => ['btn', 'btn-primary', 'submit']
    = link_to 'Cancel', '#', :class => 'btn', :data => {:dismiss => 'modal'}