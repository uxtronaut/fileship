= simple_form_for [user_file.folder, user_file], :html => {:class => 'form-horizontal'} do |form|

  = form.error_notification

  = form.input :name, :input_html => {:class => 'span3'}

  .form-actions
    = form.submit 'Rename File', :class => 'btn btn-primary'